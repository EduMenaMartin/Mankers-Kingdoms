We're wrapping up. Do the following:

1. **Update HANDOVER.md:**
   - What we did this session (bullet list, concrete outcomes)
   - What's next (top 1-3 tasks)
   - What's blocked (if anything)
   - Any decisions you need from me before next session
   - Session log: append a dated entry summarizing the session

2. **Update TODO.md:**
   - Tick off completed items
   - Add any new subtasks discovered during the work
   - If we finished the milestone, note it and prep for milestone review

3. **Update BUGS.md** if we found or fixed any bugs.

4. **Update IDEAS_BACKLOG.md** if any ideas came up that we didn't act on — with triage tags.

5. **Update CHANGELOG.md** ONLY if we hit a milestone or completed a meaningful chunk worth recording.

6. **Prepare a commit message** in Conventional Commits style referencing the milestone and any ADRs. Format the git commit command using multiple -m flags (one per paragraph) so it pastes into Windows cmd/PowerShell cleanly. Do NOT run git commit — show me the message and I'll commit via Rider or terminal.

7. **Report back:** show me the diffs of the updated .md files, the commit message, and confirm nothing else is pending.

Then stop. I'll run git commit myself.
